let uniqueTypes = []


document.addEventListener()
fetch ("data.json")
    .then (response => response.json)
    .then (elements) => {
        items.forEach 
            let restaurnat =documnet.created
            let container=document.querySelector('.restaurant-container')
        
    )
    }

    if(!uniqueTypes.includes(item.type) && item.type){
        uniqueTypes.push(item.type)
    
    if(!uni
        let price = documnet.createElement('button')

        let priceContainer = documnet.querySelector('.price-container')
    }

